# Optional ML/vision extras. Install only if needed and supported on your platform.
# NOTE: These are heavy on Raspberry Pi; many do not have wheels for 32-bit OS.

# OpenCV (vision utilities)
opencv-python

# Whisper-based subtitle extraction (either)
faster-whisper
openai-whisper

# Face pipeline (detector + embeddings)
insightface
# Use Apple Silicon build of onnxruntime on macOS arm64; otherwise use standard on other platforms
onnxruntime-silicon ; platform_system == 'Darwin' and platform_machine == 'arm64'
onnxruntime ; platform_system != 'Darwin' or platform_machine != 'arm64'
